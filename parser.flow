import arExp;
import algExp;
import rpnE;
import polymorphic;


main(){
    switch (parseArExp("(1+((27/(4+5))-5))") : Maybe<ArExp>) {
        Some(arexp): {
            aeTesting(arexp);
            rpnTesting(arexp);
            rpnPolTesting(arexp);
        }
        None():{
            println("None");     
        }
    }
    switch (parseAlgExp("(a+((b/(4+5))-c))") : Maybe<AlgExp>) {
        Some(algexp): {
            algTesting(algexp);
        }
        None():{
            println("None");     
        }
    }
    

}